//! Integration test: public API for schema parsing and code generation, and CLI binary.

use json_schema_rs::{
    CodeGenSettings, DedupeMode, JsonSchema, JsonSchemaSettings, ModelNameSource, generate_rust,
    parse_schema,
};
use std::collections::{BTreeMap, BTreeSet};
use std::fs;
use std::io::Write;
use std::path::{Path, PathBuf};
use std::process::Command;
use std::process::Stdio;

fn jsonschemars_bin() -> std::path::PathBuf {
    if let Ok(path) = std::env::var("CARGO_BIN_EXE_jsonschemars") {
        return std::path::PathBuf::from(path);
    }
    let manifest_dir =
        std::env::var("CARGO_MANIFEST_DIR").expect("CARGO_MANIFEST_DIR set by Cargo");
    let target_dir = std::env::var("CARGO_TARGET_DIR").unwrap_or_else(|_| {
        std::path::PathBuf::from(&manifest_dir)
            .join("..")
            .join("target")
            .into_os_string()
            .into_string()
            .unwrap()
    });
    let bin = std::path::Path::new(&target_dir)
        .join("debug")
        .join("jsonschemars");
    assert!(
        bin.exists(),
        "jsonschemars binary not found at {}",
        bin.display()
    );
    bin
}

#[test]
fn cli_generate_rust_single_file_to_output_dir() {
    let schema_json =
        r#"{"type":"object","properties":{"id":{"type":"string"}},"required":["id"]}"#;
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let schema_path = temp_dir.path().join("schema.json");
    std::fs::write(&schema_path, schema_json).expect("write schema");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            schema_path.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(
        output.status.success(),
        "exit success: stderr={}",
        String::from_utf8_lossy(&output.stderr)
    );
    let out_path = out_dir.path().join("schema.rs");
    let actual = std::fs::read_to_string(&out_path).expect("read output");
    let expected = r"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    pub id: String,
}

";
    assert_eq!(expected, actual);
}

#[test]
fn cli_generate_rust_enum_property() {
    let schema_json = r#"{"type":"object","properties":{"status":{"enum":["open","closed"]}},"required":["status"]}"#;
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let schema_path = temp_dir.path().join("schema.json");
    std::fs::write(&schema_path, schema_json).expect("write schema");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            schema_path.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(
        output.status.success(),
        "exit success: stderr={}",
        String::from_utf8_lossy(&output.stderr)
    );
    let out_path = out_dir.path().join("schema.rs");
    let actual = std::fs::read_to_string(&out_path).expect("read output");
    assert!(actual.contains("pub enum Status"));
    assert!(actual.contains("pub struct Root"));
    assert!(actual.contains("pub status: Status"));
    assert!(actual.contains("Open"));
    assert!(actual.contains("Closed"));
}

#[test]
fn cli_generate_rust_enum_collision() {
    let schema_json = r#"{"type":"object","properties":{"t":{"enum":["a","A"]}},"required":["t"]}"#;
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let schema_path = temp_dir.path().join("schema.json");
    std::fs::write(&schema_path, schema_json).expect("write schema");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            schema_path.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(
        output.status.success(),
        "exit success: stderr={}",
        String::from_utf8_lossy(&output.stderr)
    );
    let out_path = out_dir.path().join("schema.rs");
    let actual = std::fs::read_to_string(&out_path).expect("read output");
    assert!(actual.contains("pub enum T"));
    assert!(actual.contains("A_0"));
    assert!(actual.contains("A_1"));
    assert!(actual.contains("pub t: T"));
}

#[test]
fn cli_generate_rust_enum_dedupe() {
    let schema_json = r#"{"type":"object","properties":{"a":{"enum":["x","y"]},"b":{"enum":["x","y"]}},"required":["a"]}"#;
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let schema_path = temp_dir.path().join("schema.json");
    std::fs::write(&schema_path, schema_json).expect("write schema");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            schema_path.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(
        output.status.success(),
        "exit success: stderr={}",
        String::from_utf8_lossy(&output.stderr)
    );
    let out_path = out_dir.path().join("schema.rs");
    let actual = std::fs::read_to_string(&out_path).expect("read output");
    assert!(actual.contains("pub enum A"));
    assert!(actual.contains("pub a: A"));
    assert!(actual.contains("pub b: Option<A>"));
}

#[test]
fn cli_generate_rust_enum_duplicate_values() {
    let schema_json = r#"{"type":"object","properties":{"t":{"enum":["A","A","A","a","a","a","a","a","a","a"]}},"required":["t"]}"#;
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let schema_path = temp_dir.path().join("schema.json");
    std::fs::write(&schema_path, schema_json).expect("write schema");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            schema_path.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(
        output.status.success(),
        "exit success: stderr={}",
        String::from_utf8_lossy(&output.stderr)
    );
    let out_path = out_dir.path().join("schema.rs");
    let actual = std::fs::read_to_string(&out_path).expect("read output");
    assert!(actual.contains("pub enum T"));
    assert!(actual.contains("A_0"));
    assert!(actual.contains("A_1"));
    assert!(actual.contains("pub t: T"));
}

#[test]
fn cli_generate_rust_integer_property() {
    let schema_json =
        r#"{"type":"object","properties":{"count":{"type":"integer"}},"required":["count"]}"#;
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let schema_path = temp_dir.path().join("schema.json");
    std::fs::write(&schema_path, schema_json).expect("write schema");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            schema_path.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(
        output.status.success(),
        "exit success: stderr={}",
        String::from_utf8_lossy(&output.stderr)
    );
    let out_path = out_dir.path().join("schema.rs");
    let actual = std::fs::read_to_string(&out_path).expect("read output");
    let expected = r"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    pub count: i64,
}

";
    assert_eq!(expected, actual);
}

#[test]
fn cli_generate_rust_optional_integer() {
    let schema_json = r#"{"type":"object","properties":{"count":{"type":"integer"}}}"#;
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let schema_path = temp_dir.path().join("schema.json");
    std::fs::write(&schema_path, schema_json).expect("write schema");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            schema_path.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(
        output.status.success(),
        "exit success: stderr={}",
        String::from_utf8_lossy(&output.stderr)
    );
    let out_path = out_dir.path().join("schema.rs");
    let actual = std::fs::read_to_string(&out_path).expect("read output");
    let expected = r"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    pub count: Option<i64>,
}

";
    assert_eq!(expected, actual);
}

#[test]
fn cli_generate_rust_integer_with_min_max_emits_u8() {
    let schema_json = r#"{"type":"object","properties":{"byte":{"type":"integer","minimum":0,"maximum":255}},"required":["byte"]}"#;
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let schema_path = temp_dir.path().join("schema.json");
    std::fs::write(&schema_path, schema_json).expect("write schema");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            schema_path.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(
        output.status.success(),
        "exit success: stderr={}",
        String::from_utf8_lossy(&output.stderr)
    );
    let out_path = out_dir.path().join("schema.rs");
    let actual = std::fs::read_to_string(&out_path).expect("read output");
    let expected = r"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    pub byte: u8,
}

";
    assert_eq!(expected, actual);
}

#[test]
fn cli_generate_rust_number_with_min_max_emits_f32() {
    let schema_json = r#"{"type":"object","properties":{"value":{"type":"number","minimum":0.5,"maximum":100.5}},"required":["value"]}"#;
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let schema_path = temp_dir.path().join("schema.json");
    std::fs::write(&schema_path, schema_json).expect("write schema");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            schema_path.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(
        output.status.success(),
        "exit success: stderr={}",
        String::from_utf8_lossy(&output.stderr)
    );
    let out_path = out_dir.path().join("schema.rs");
    let actual = std::fs::read_to_string(&out_path).expect("read output");
    let expected = r"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    pub value: f32,
}

";
    assert_eq!(expected, actual);
}

#[test]
fn cli_generate_rust_float_property() {
    let schema_json =
        r#"{"type":"object","properties":{"value":{"type":"number"}},"required":["value"]}"#;
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let schema_path = temp_dir.path().join("schema.json");
    std::fs::write(&schema_path, schema_json).expect("write schema");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            schema_path.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(
        output.status.success(),
        "exit success: stderr={}",
        String::from_utf8_lossy(&output.stderr)
    );
    let out_path = out_dir.path().join("schema.rs");
    let actual = std::fs::read_to_string(&out_path).expect("read output");
    let expected = r"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    pub value: f64,
}

";
    assert_eq!(expected, actual);
}

#[test]
fn cli_generate_rust_optional_float() {
    let schema_json = r#"{"type":"object","properties":{"value":{"type":"number"}}}"#;
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let schema_path = temp_dir.path().join("schema.json");
    std::fs::write(&schema_path, schema_json).expect("write schema");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            schema_path.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(
        output.status.success(),
        "exit success: stderr={}",
        String::from_utf8_lossy(&output.stderr)
    );
    let out_path = out_dir.path().join("schema.rs");
    let actual = std::fs::read_to_string(&out_path).expect("read output");
    let expected = r"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    pub value: Option<f64>,
}

";
    assert_eq!(expected, actual);
}

#[test]
fn cli_generate_rust_array_required() {
    let schema_json = r#"{"type":"object","properties":{"tags":{"type":"array","items":{"type":"string"}}},"required":["tags"]}"#;
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let schema_path = temp_dir.path().join("schema.json");
    std::fs::write(&schema_path, schema_json).expect("write schema");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            schema_path.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(
        output.status.success(),
        "exit success: stderr={}",
        String::from_utf8_lossy(&output.stderr)
    );
    let out_path = out_dir.path().join("schema.rs");
    let actual = std::fs::read_to_string(&out_path).expect("read output");
    let expected = r"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    pub tags: Vec<String>,
}

";
    assert_eq!(expected, actual);
}

#[test]
fn cli_generate_rust_array_optional() {
    let schema_json =
        r#"{"type":"object","properties":{"tags":{"type":"array","items":{"type":"string"}}}}"#;
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let schema_path = temp_dir.path().join("schema.json");
    std::fs::write(&schema_path, schema_json).expect("write schema");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            schema_path.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(
        output.status.success(),
        "exit success: stderr={}",
        String::from_utf8_lossy(&output.stderr)
    );
    let out_path = out_dir.path().join("schema.rs");
    let actual = std::fs::read_to_string(&out_path).expect("read output");
    assert!(actual.contains("pub tags: Option<Vec<String>>"));
}

#[test]
fn cli_generate_rust_array_unique_items_true() {
    let schema_json = r#"{"type":"object","properties":{"tags":{"type":"array","items":{"type":"string"},"uniqueItems":true}},"required":["tags"]}"#;
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let schema_path = temp_dir.path().join("schema.json");
    std::fs::write(&schema_path, schema_json).expect("write schema");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            schema_path.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(
        output.status.success(),
        "exit success: stderr={}",
        String::from_utf8_lossy(&output.stderr)
    );
    let out_path = out_dir.path().join("schema.rs");
    let actual = std::fs::read_to_string(&out_path).expect("read output");
    assert!(
        actual.contains("pub tags: HashSet<String>"),
        "expected HashSet<String>: {actual}"
    );
    assert!(
        actual.contains("use std::collections::HashSet"),
        "expected HashSet use: {actual}"
    );
}

#[test]
fn cli_generate_rust_array_min_items_max_items() {
    let schema_json = r#"{"type":"object","properties":{"tags":{"type":"array","items":{"type":"string"},"minItems":2,"maxItems":5}},"required":["tags"]}"#;
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let schema_path = temp_dir.path().join("schema.json");
    std::fs::write(&schema_path, schema_json).expect("write schema");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            schema_path.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(
        output.status.success(),
        "exit success: stderr={}",
        String::from_utf8_lossy(&output.stderr)
    );
    let out_path = out_dir.path().join("schema.rs");
    let actual = std::fs::read_to_string(&out_path).expect("read output");
    assert!(
        actual.contains("#[to_json_schema(min_items = 2, max_items = 5)]"),
        "expected min_items/max_items attribute: {actual}"
    );
    assert!(actual.contains("pub tags: Vec<String>"));
}

#[test]
fn cli_generate_rust_string_min_length_max_length() {
    let schema_json = r#"{"type":"object","properties":{"name":{"type":"string","minLength":2,"maxLength":50}},"required":["name"]}"#;
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let schema_path = temp_dir.path().join("schema.json");
    std::fs::write(&schema_path, schema_json).expect("write schema");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            schema_path.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(
        output.status.success(),
        "exit success: stderr={}",
        String::from_utf8_lossy(&output.stderr)
    );
    let out_path = out_dir.path().join("schema.rs");
    let actual = std::fs::read_to_string(&out_path).expect("read output");
    assert!(
        actual.contains("#[to_json_schema(min_length = 2, max_length = 50)]"),
        "expected min_length/max_length attribute: {actual}"
    );
    assert!(actual.contains("pub name: String"));
}

#[test]
fn cli_generate_unsupported_language_exit_nonzero_and_stderr() {
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let schema_path = temp_dir.path().join("x.json");
    std::fs::write(
        &schema_path,
        r#"{"type":"object","properties":{"x":{"type":"string"}}}"#,
    )
    .expect("write schema");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "python",
            "-o",
            out_dir.path().to_str().unwrap(),
            schema_path.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(!output.status.success(), "generate python should fail");
    let stderr = String::from_utf8(output.stderr).expect("utf8 stderr");
    assert!(
        stderr.contains("unsupported"),
        "stderr should mention unsupported language: {stderr}"
    );
}

#[test]
fn cli_generate_rust_multiple_files_mirror_paths() {
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let a_path = temp_dir.path().join("a.json");
    let b_path = temp_dir.path().join("b.json");
    std::fs::write(
        &a_path,
        r#"{"type":"object","properties":{"a":{"type":"string"}}}"#,
    )
    .expect("write a");
    std::fs::write(
        &b_path,
        r#"{"type":"object","properties":{"b":{"type":"string"}}}"#,
    )
    .expect("write b");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            a_path.to_str().unwrap(),
            b_path.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(
        output.status.success(),
        "stderr={}",
        String::from_utf8_lossy(&output.stderr)
    );
    let a_rs = std::fs::read_to_string(out_dir.path().join("a.rs")).expect("read a.rs");
    let b_rs = std::fs::read_to_string(out_dir.path().join("b.rs")).expect("read b.rs");
    assert!(
        a_rs.contains("pub a: Option<String>"),
        "a.rs should have field a"
    );
    assert!(
        b_rs.contains("pub b: Option<String>"),
        "b.rs should have field b"
    );
}

#[test]
fn cli_generate_rust_dir_recursive_mirrors_structure() {
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let nested = temp_dir.path().join("nested");
    std::fs::create_dir_all(&nested).expect("create nested");
    std::fs::write(
        temp_dir.path().join("root.json"),
        r#"{"type":"object","properties":{"x":{"type":"string"}}}"#,
    )
    .expect("write root");
    std::fs::write(
        nested.join("child.json"),
        r#"{"type":"object","properties":{"y":{"type":"string"}}}"#,
    )
    .expect("write child");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            temp_dir.path().to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(
        output.status.success(),
        "stderr={}",
        String::from_utf8_lossy(&output.stderr)
    );
    let root_rs = std::fs::read_to_string(out_dir.path().join("root.rs")).expect("read root.rs");
    let child_rs = std::fs::read_to_string(out_dir.path().join("nested").join("child.rs"))
        .expect("read nested/child.rs");
    assert!(
        root_rs.contains("pub x: Option<String>"),
        "root.rs should have field x"
    );
    assert!(
        child_rs.contains("pub y: Option<String>"),
        "nested/child.rs should have field y"
    );
    let root_mod = std::fs::read_to_string(out_dir.path().join("mod.rs")).expect("read mod.rs");
    let nested_mod = std::fs::read_to_string(out_dir.path().join("nested").join("mod.rs"))
        .expect("read nested/mod.rs");
    assert!(
        root_mod.contains("pub mod nested;") && root_mod.contains("pub mod root;"),
        "root mod.rs should re-export nested and root: {root_mod}"
    );
    assert!(
        nested_mod.contains("pub mod child;"),
        "nested/mod.rs should re-export child: {nested_mod}"
    );
}

#[test]
fn cli_generate_rust_ingestion_single_failure_logs_path_and_exits_nonzero() {
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let bad_path = temp_dir.path().join("bad.json");
    std::fs::write(&bad_path, r#"{"type":"object","properties":{}"#).expect("write invalid json");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            bad_path.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(!output.status.success(), "should exit non-zero");
    let stderr = String::from_utf8_lossy(&output.stderr);
    assert!(
        stderr.contains("bad.json"),
        "stderr should contain failing file path: {stderr}"
    );
    assert!(
        stderr.contains("invalid JSON Schema") || stderr.contains("EOF"),
        "stderr should contain error reason: {stderr}"
    );
}

#[test]
fn cli_generate_rust_ingestion_multiple_failures_logs_all_and_exits_nonzero() {
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let bad1 = temp_dir.path().join("bad1.json");
    let bad2 = temp_dir.path().join("bad2.json");
    std::fs::write(&bad1, r#"{"type":"object"#).expect("write bad1");
    std::fs::write(&bad2, r"not json").expect("write bad2");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            bad1.to_str().unwrap(),
            bad2.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(!output.status.success(), "should exit non-zero");
    let stderr = String::from_utf8_lossy(&output.stderr);
    let expected_bad1 = stderr.contains("bad1.json");
    let expected_bad2 = stderr.contains("bad2.json");
    assert!(
        expected_bad1 && expected_bad2,
        "stderr should contain both failing file paths; stderr: {stderr}"
    );
}

#[test]
fn cli_generate_rust_ingestion_partial_success_no_output_written() {
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let good_path = temp_dir.path().join("good.json");
    let bad_path = temp_dir.path().join("bad.json");
    std::fs::write(
        &good_path,
        r#"{"type":"object","properties":{"x":{"type":"string"}}}"#,
    )
    .expect("write good");
    std::fs::write(&bad_path, r#"{"type":"object"#).expect("write bad");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            good_path.to_str().unwrap(),
            bad_path.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(!output.status.success(), "should exit non-zero");
    let stderr = String::from_utf8_lossy(&output.stderr);
    assert!(
        stderr.contains("bad.json"),
        "stderr should log failing file: {stderr}"
    );
    let good_rs = out_dir.path().join("good.rs");
    assert!(
        !good_rs.exists(),
        "no output should be written when any ingestion fails: {good_rs:?} should not exist"
    );
}

#[test]
fn cli_generate_rust_hyphenated_paths_sanitized_and_mod_rs_emitted() {
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let sub_dir = temp_dir.path().join("sub-dir");
    std::fs::create_dir_all(&sub_dir).expect("create sub-dir");
    std::fs::write(
        temp_dir.path().join("schema-1.json"),
        r#"{"type":"object","properties":{"a":{"type":"string"}}}"#,
    )
    .expect("write schema-1");
    std::fs::write(
        sub_dir.join("schema-2.json"),
        r#"{"type":"object","properties":{"b":{"type":"string"}}}"#,
    )
    .expect("write schema-2");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            temp_dir.path().to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(
        output.status.success(),
        "stderr={}",
        String::from_utf8_lossy(&output.stderr)
    );
    let schema_1_rs = out_dir.path().join("schema_1.rs");
    let sub_dir_schema_2 = out_dir.path().join("sub_dir").join("schema_2.rs");
    assert!(
        schema_1_rs.exists(),
        "schema_1.rs should exist (sanitized from schema-1.json)"
    );
    assert!(
        sub_dir_schema_2.exists(),
        "sub_dir/schema_2.rs should exist (sanitized path)"
    );
    let root_mod = std::fs::read_to_string(out_dir.path().join("mod.rs")).expect("read mod.rs");
    let expected_root =
        root_mod.contains("pub mod schema_1;") && root_mod.contains("pub mod sub_dir;");
    assert!(
        expected_root,
        "root mod.rs should declare schema_1 and sub_dir: {root_mod}"
    );
    let sub_dir_mod = std::fs::read_to_string(out_dir.path().join("sub_dir").join("mod.rs"))
        .expect("read sub_dir/mod.rs");
    assert!(
        sub_dir_mod.contains("pub mod schema_2;"),
        "sub_dir/mod.rs should declare schema_2: {sub_dir_mod}"
    );
}

#[test]
fn cli_generate_rust_optional_string() {
    let schema_json = r#"{"type":"object","properties":{"name":{"type":"string"}}}"#;
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let schema_path = temp_dir.path().join("schema.json");
    std::fs::write(&schema_path, schema_json).expect("write schema");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            schema_path.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(
        output.status.success(),
        "exit success: stderr={}",
        String::from_utf8_lossy(&output.stderr)
    );
    let out_path = out_dir.path().join("schema.rs");
    let actual = std::fs::read_to_string(&out_path).expect("read output");
    let expected = r"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    pub name: Option<String>,
}

";
    assert_eq!(expected, actual);
}

#[test]
fn cli_generate_rust_description() {
    let schema_json = r#"{"type":"object","description":"Root type","properties":{"name":{"type":"string","description":"User name"}}}"#;
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let schema_path = temp_dir.path().join("schema.json");
    std::fs::write(&schema_path, schema_json).expect("write schema");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            schema_path.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(
        output.status.success(),
        "exit success: stderr={}",
        String::from_utf8_lossy(&output.stderr)
    );
    let out_path = out_dir.path().join("schema.rs");
    let actual = std::fs::read_to_string(&out_path).expect("read output");
    let expected = r"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

/// Root type
#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    /// User name
    pub name: Option<String>,
}

";
    assert_eq!(expected, actual);
}

#[test]
fn cli_generate_rust_nested_object() {
    let schema_json = r#"{"type":"object","properties":{"address":{"type":"object","properties":{"city":{"type":"string"},"street_address":{"type":"string"}}}}}"#;
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let schema_path = temp_dir.path().join("schema.json");
    std::fs::write(&schema_path, schema_json).expect("write schema");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            schema_path.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(
        output.status.success(),
        "stderr={}",
        String::from_utf8_lossy(&output.stderr)
    );
    let out_path = out_dir.path().join("schema.rs");
    let actual = std::fs::read_to_string(&out_path).expect("read output");
    assert!(
        actual.contains("pub struct Address"),
        "output should contain Address struct: {actual}"
    );
    assert!(
        actual.contains("pub address: Option<Address>"),
        "output should contain address field: {actual}"
    );
    assert!(
        actual.contains("pub city: Option<String>")
            && actual.contains("pub street_address: Option<String>"),
        "output should contain nested fields: {actual}"
    );
}

#[test]
fn cli_generate_rust_hyphenated_property_key() {
    let schema_json = r#"{"type":"object","properties":{"foo-bar":{"type":"string"}}}"#;
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let schema_path = temp_dir.path().join("schema.json");
    std::fs::write(&schema_path, schema_json).expect("write schema");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            schema_path.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(
        output.status.success(),
        "stderr={}",
        String::from_utf8_lossy(&output.stderr)
    );
    let out_path = out_dir.path().join("schema.rs");
    let actual = std::fs::read_to_string(&out_path).expect("read output");
    let expected = r#"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    #[serde(rename = "foo-bar")]
    pub foo_bar: Option<String>,
}

"#;
    assert_eq!(expected, actual);
}

#[test]
fn cli_generate_rust_dedupe_two_identical_schemas() {
    let schema_json = r#"{"type":"object","properties":{"id":{"type":"string"}}}"#;
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let a_path = temp_dir.path().join("a.json");
    let b_path = temp_dir.path().join("b.json");
    std::fs::write(&a_path, schema_json).expect("write a");
    std::fs::write(&b_path, schema_json).expect("write b");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            a_path.to_str().unwrap(),
            b_path.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(
        output.status.success(),
        "stderr={}",
        String::from_utf8_lossy(&output.stderr)
    );
    let shared_rs = out_dir.path().join("shared.rs");
    assert!(
        shared_rs.exists(),
        "shared.rs should exist when two identical schemas"
    );
    let shared_content = std::fs::read_to_string(&shared_rs).expect("read shared.rs");
    assert!(
        shared_content.contains("pub struct Root"),
        "shared.rs should contain Root struct: {shared_content}"
    );
    let a_rs = std::fs::read_to_string(out_dir.path().join("a.rs")).expect("read a.rs");
    assert!(
        a_rs.contains("pub use crate::shared::Root"),
        "a.rs should re-export shared Root: {a_rs}"
    );
}

#[test]
fn cli_generate_rust_model_name_source_property_key() {
    let schema_json = r#"{"type":"object","properties":{"address":{"type":"object","title":"FooBar","properties":{"city":{"type":"string"}}}}}"#;
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let schema_path = temp_dir.path().join("schema.json");
    std::fs::write(&schema_path, schema_json).expect("write schema");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            "--cgs-model-name-source",
            "property-key",
            schema_path.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(
        output.status.success(),
        "stderr={}",
        String::from_utf8_lossy(&output.stderr)
    );
    let out_path = out_dir.path().join("schema.rs");
    let actual = std::fs::read_to_string(&out_path).expect("read output");
    assert!(
        actual.contains("pub struct Address "),
        "with property-key nested struct should be named Address: {actual}"
    );
    assert!(
        !actual.contains("struct FooBar "),
        "with property-key title FooBar should not be used: {actual}"
    );
}

#[test]
fn cli_generate_rust_root_not_object_exits_nonzero() {
    let schema_json = r#"{"type":"string"}"#;
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let schema_path = temp_dir.path().join("schema.json");
    std::fs::write(&schema_path, schema_json).expect("write schema");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            schema_path.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(
        !output.status.success(),
        "root not object should exit non-zero"
    );
    let stderr = String::from_utf8_lossy(&output.stderr);
    assert!(
        stderr.contains("RootNotObject") || stderr.contains("root") || stderr.contains("object"),
        "stderr should mention error: {stderr}"
    );
}

#[test]
fn cli_generate_rust_deep_schema_succeeds() {
    let mut inner: serde_json::Value = serde_json::json!({
        "type": "object",
        "properties": { "value": { "type": "string" } }
    });
    for _ in 0..20 {
        inner = serde_json::json!({
            "type": "object",
            "properties": { "child": inner }
        });
    }
    let schema_json = serde_json::to_string(&inner).expect("serialize schema");
    let temp_dir = tempfile::tempdir().expect("temp dir");
    let schema_path = temp_dir.path().join("schema.json");
    std::fs::write(&schema_path, schema_json).expect("write schema");
    let out_dir = tempfile::tempdir().expect("temp out dir");
    let output = Command::new(jsonschemars_bin())
        .args([
            "generate",
            "rust",
            "-o",
            out_dir.path().to_str().unwrap(),
            schema_path.to_str().unwrap(),
        ])
        .output()
        .expect("run jsonschemars");
    assert!(
        output.status.success(),
        "deep schema should succeed: stderr={}",
        String::from_utf8_lossy(&output.stderr)
    );
}

#[test]
fn cli_validate_valid_payload_exit_zero() {
    let schema_json = r#"{"type":"object","properties":{"x":{"type":"string"}}}"#;
    let mut schema_file = tempfile::NamedTempFile::new().expect("temp schema");
    std::io::Write::write_all(&mut schema_file, schema_json.as_bytes()).expect("write schema");
    schema_file.flush().expect("flush schema");
    let payload_json = r#"{"x":"ok"}"#;
    let mut child = Command::new(jsonschemars_bin())
        .args(["validate", "-s", schema_file.path().to_str().unwrap()])
        .stdin(Stdio::piped())
        .stdout(Stdio::piped())
        .stderr(Stdio::piped())
        .spawn()
        .expect("spawn jsonschemars");
    std::io::Write::write_all(child.stdin.as_mut().unwrap(), payload_json.as_bytes())
        .expect("write payload");
    let output = child.wait_with_output().expect("wait jsonschemars");
    assert!(
        output.status.success(),
        "validate should succeed: stderr={}",
        String::from_utf8_lossy(&output.stderr)
    );
}

#[test]
fn cli_validate_invalid_payload_exit_nonzero_and_stderr() {
    let schema_json = r#"{"type":"object","required":["id"]}"#;
    let payload_json = r"{}";
    let mut schema_file = tempfile::NamedTempFile::new().expect("temp schema");
    std::io::Write::write_all(&mut schema_file, schema_json.as_bytes()).expect("write schema");
    schema_file.flush().expect("flush schema");
    let mut child = Command::new(jsonschemars_bin())
        .args(["validate", "-s", schema_file.path().to_str().unwrap()])
        .stdin(Stdio::piped())
        .stdout(Stdio::piped())
        .stderr(Stdio::piped())
        .spawn()
        .expect("spawn jsonschemars");
    std::io::Write::write_all(child.stdin.as_mut().unwrap(), payload_json.as_bytes())
        .expect("write payload");
    let output = child.wait_with_output().expect("wait jsonschemars");
    assert!(!output.status.success(), "validate should fail");
    let stderr = String::from_utf8(output.stderr).expect("utf8 stderr");
    assert!(
        stderr.contains("missing required"),
        "stderr should mention missing required: {stderr}"
    );
}

#[test]
fn integration_parse_and_generate() {
    let json = r#"{"type":"object","properties":{"id":{"type":"string"}},"required":["id"]}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let actual = String::from_utf8(output.per_schema[0].clone()).expect("utf8");
    let expected = r"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    pub id: String,
}

";
    assert_eq!(expected, actual);
}

#[test]
fn integration_parse_and_generate_optional_string() {
    let json = r#"{"type":"object","properties":{"name":{"type":"string"}}}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let actual = String::from_utf8(output.per_schema[0].clone()).expect("utf8");
    let expected = r"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    pub name: Option<String>,
}

";
    assert_eq!(expected, actual);
}

#[test]
fn integration_parse_and_generate_enum_required() {
    let json = r#"{"type":"object","properties":{"status":{"enum":["open","closed"]}},"required":["status"]}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let actual = String::from_utf8(output.per_schema[0].clone()).expect("utf8");
    let expected = r#"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub enum Status {
    #[serde(rename = "closed")]
    Closed,
    #[serde(rename = "open")]
    Open,
}

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    pub status: Status,
}

"#;
    assert_eq!(expected, actual);
}

#[test]
fn integration_parse_and_generate_description_struct_and_field() {
    let json = r#"{"type":"object","description":"Root type","properties":{"name":{"type":"string","description":"User name"}}}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let actual = String::from_utf8(output.per_schema[0].clone()).expect("utf8");
    let expected = r"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

/// Root type
#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    /// User name
    pub name: Option<String>,
}

";
    assert_eq!(expected, actual);
}

#[test]
fn integration_parse_and_generate_enum_optional() {
    let json = r#"{"type":"object","properties":{"level":{"enum":["low","medium","high"]}}}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let actual = String::from_utf8(output.per_schema[0].clone()).expect("utf8");
    assert!(actual.contains("pub enum Level"));
    assert!(actual.contains("pub level: Option<Level>"));
    assert!(actual.contains("Low"));
    assert!(actual.contains("Medium"));
    assert!(actual.contains("High"));
}

#[test]
fn integration_parse_and_generate_enum_duplicate_values() {
    let json = r#"{"type":"object","properties":{"t":{"enum":["A","A","A","a","a","a","a","a","a","a"]}},"required":["t"]}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let actual = String::from_utf8(output.per_schema[0].clone()).expect("utf8");
    let expected = r#"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub enum T {
    #[serde(rename = "A")]
    A_0,
    #[serde(rename = "a")]
    A_1,
}

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    pub t: T,
}

"#;
    assert_eq!(expected, actual);
}

#[test]
fn integration_parse_and_generate_nested_object() {
    let json = r#"{
          "type": "object",
          "properties": {
            "first_name": { "type": "string" },
            "address": {
              "type": "object",
              "properties": {
                "street_address": { "type": "string" },
                "city": { "type": "string" }
              }
            }
          }
        }"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let actual = String::from_utf8(output.per_schema[0].clone()).expect("utf8");
    let expected = r"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Address {
    pub city: Option<String>,
    pub street_address: Option<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    pub address: Option<Address>,
    pub first_name: Option<String>,
}

";
    assert_eq!(expected, actual);
}

#[test]
fn integration_parse_and_generate_required_integer() {
    let json =
        r#"{"type":"object","properties":{"count":{"type":"integer"}},"required":["count"]}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let actual = String::from_utf8(output.per_schema[0].clone()).expect("utf8");
    let expected = r"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    pub count: i64,
}

";
    assert_eq!(expected, actual);
}

#[test]
fn integration_parse_and_generate_optional_integer() {
    let json = r#"{"type":"object","properties":{"count":{"type":"integer"}}}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let actual = String::from_utf8(output.per_schema[0].clone()).expect("utf8");
    let expected = r"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    pub count: Option<i64>,
}

";
    assert_eq!(expected, actual);
}

#[test]
fn integration_parse_and_generate_required_float() {
    let json = r#"{"type":"object","properties":{"value":{"type":"number"}},"required":["value"]}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let actual = String::from_utf8(output.per_schema[0].clone()).expect("utf8");
    let expected = r"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    pub value: f64,
}

";
    assert_eq!(expected, actual);
}

#[test]
fn integration_parse_and_generate_optional_float() {
    let json = r#"{"type":"object","properties":{"value":{"type":"number"}}}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let actual = String::from_utf8(output.per_schema[0].clone()).expect("utf8");
    let expected = r"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    pub value: Option<f64>,
}

";
    assert_eq!(expected, actual);
}

#[test]
fn integration_parse_and_generate_integer_with_minimum_maximum_emits_u8() {
    let json = r#"{"type":"object","properties":{"byte":{"type":"integer","minimum":0,"maximum":255}},"required":["byte"]}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let actual = String::from_utf8(output.per_schema[0].clone()).expect("utf8");
    let expected = r"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    pub byte: u8,
}

";
    assert_eq!(expected, actual);
}

#[test]
fn integration_parse_and_generate_number_with_min_max_emits_f32() {
    let json = r#"{"type":"object","properties":{"value":{"type":"number","minimum":0.5,"maximum":100.5}},"required":["value"]}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let actual = String::from_utf8(output.per_schema[0].clone()).expect("utf8");
    let expected = r"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    pub value: f32,
}

";
    assert_eq!(expected, actual);
}

#[test]
fn integration_parse_and_generate_array_required() {
    let json = r#"{"type":"object","properties":{"tags":{"type":"array","items":{"type":"string"}}},"required":["tags"]}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let actual = String::from_utf8(output.per_schema[0].clone()).expect("utf8");
    let expected = r"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    pub tags: Vec<String>,
}

";
    assert_eq!(expected, actual);
}

#[test]
fn integration_parse_and_generate_array_unique_items_true() {
    let json = r#"{"type":"object","properties":{"tags":{"type":"array","items":{"type":"string"},"uniqueItems":true}},"required":["tags"]}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let actual = String::from_utf8(output.per_schema[0].clone()).expect("utf8");
    assert!(
        actual.contains("pub tags: HashSet<String>"),
        "expected HashSet<String> for uniqueItems true: {actual}"
    );
    assert!(
        actual.contains("use std::collections::HashSet"),
        "expected HashSet use: {actual}"
    );
}

#[test]
fn integration_parse_and_generate_array_optional() {
    let json =
        r#"{"type":"object","properties":{"tags":{"type":"array","items":{"type":"string"}}}}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let actual = String::from_utf8(output.per_schema[0].clone()).expect("utf8");
    let expected = r"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    pub tags: Option<Vec<String>>,
}

";
    assert_eq!(expected, actual);
}

#[test]
fn integration_parse_and_generate_array_min_items_max_items() {
    let json = r#"{"type":"object","properties":{"tags":{"type":"array","items":{"type":"string"},"minItems":2,"maxItems":5}},"required":["tags"]}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let actual = String::from_utf8(output.per_schema[0].clone()).expect("utf8");
    let expected = r"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    #[to_json_schema(min_items = 2, max_items = 5)]
    pub tags: Vec<String>,
}

";
    assert_eq!(expected, actual);
}

#[test]
fn integration_parse_and_generate_string_min_length() {
    let json = r#"{"type":"object","properties":{"name":{"type":"string","minLength":2}},"required":["name"]}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let actual = String::from_utf8(output.per_schema[0].clone()).expect("utf8");
    let expected = r"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    #[to_json_schema(min_length = 2)]
    pub name: String,
}

";
    assert_eq!(expected, actual);
}

#[test]
fn integration_parse_and_generate_string_max_length() {
    let json = r#"{"type":"object","properties":{"name":{"type":"string","maxLength":50}},"required":["name"]}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let actual = String::from_utf8(output.per_schema[0].clone()).expect("utf8");
    let expected = r"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    #[to_json_schema(max_length = 50)]
    pub name: String,
}

";
    assert_eq!(expected, actual);
}

#[test]
fn integration_parse_and_generate_string_min_length_max_length() {
    let json = r#"{"type":"object","properties":{"name":{"type":"string","minLength":2,"maxLength":50}},"required":["name"]}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let actual = String::from_utf8(output.per_schema[0].clone()).expect("utf8");
    let expected = r"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    #[to_json_schema(min_length = 2, max_length = 50)]
    pub name: String,
}

";
    assert_eq!(expected, actual);
}

#[test]
fn integration_parse_and_generate_string_min_length_max_length_optional() {
    let json =
        r#"{"type":"object","properties":{"name":{"type":"string","minLength":2,"maxLength":50}}}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let actual = String::from_utf8(output.per_schema[0].clone()).expect("utf8");
    let expected = r"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    #[to_json_schema(min_length = 2, max_length = 50)]
    pub name: Option<String>,
}

";
    assert_eq!(expected, actual);
}

#[test]
fn integration_parse_and_generate_array_of_objects() {
    let json = r#"{"type":"object","properties":{"items":{"type":"array","items":{"type":"object","properties":{"name":{"type":"string"}}}}},"required":["items"]}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let actual = String::from_utf8(output.per_schema[0].clone()).expect("utf8");
    let expected = r"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Items {
    pub name: Option<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    pub items: Vec<Items>,
}

";
    assert_eq!(expected, actual);
}

#[test]
fn integration_parse_and_generate_hyphenated_property() {
    let json = r#"{"type":"object","properties":{"foo-bar":{"type":"string"}}}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let actual = String::from_utf8(output.per_schema[0].clone()).expect("utf8");
    let expected = r#"//! Generated by json-schema-rs. Do not edit manually.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, json_schema_rs_macro::ToJsonSchema)]
pub struct Root {
    #[serde(rename = "foo-bar")]
    pub foo_bar: Option<String>,
}

"#;
    assert_eq!(expected, actual);
}

#[test]
fn round_trip_generated_rust_json_schema_matches_original() {
    let schema_json = r#"{"type":"object","properties":{"id":{"type":"string"},"name":{"type":"string"}},"required":["id"],"title":"Root"}"#;
    let settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let original: JsonSchema = parse_schema(schema_json, &settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output =
        generate_rust(std::slice::from_ref(&original), &code_gen_settings).expect("generate");
    let generated = output.per_schema[0].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), &generated).expect("write lib.rs");
    let main_rs = r#"
fn main() {
    use json_schema_rs::{JsonSchema, ToJsonSchema, parse_schema, JsonSchemaSettings};
    let schema = compile_test::Root::json_schema();
    let json: String = (&schema).try_into().expect("serialize");
    let settings = JsonSchemaSettings::builder().build();
    let reparsed: JsonSchema = parse_schema(&json, &settings).expect("parse");
    assert_eq!(schema, reparsed, "round-trip Root::json_schema() must match");
}
"#;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn generated_rust_required_float_builds_and_deserializes() {
    let schema_json =
        r#"{"type":"object","properties":{"value":{"type":"number"}},"required":["value"]}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(schema_json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let generated = output.per_schema[0].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), &generated).expect("write lib.rs");
    let main_rs = r##"fn main() {
    let root: compile_test::Root = serde_json::from_str(r#"{"value":3.14}"#).unwrap();
    assert_eq!(root.value, 3.14);
}
"##;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn generated_rust_array_required_builds_and_deserializes() {
    let schema_json = r#"{"type":"object","properties":{"tags":{"type":"array","items":{"type":"string"}}},"required":["tags"]}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(schema_json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let generated = output.per_schema[0].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), &generated).expect("write lib.rs");
    let main_rs = r##"fn main() {
    let root: compile_test::Root = serde_json::from_str(r#"{"tags":["a","b","c"]}"#).unwrap();
    assert_eq!(root.tags, vec!["a".to_string(), "b".to_string(), "c".to_string()]);
}
"##;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn generated_rust_unique_items_builds_and_deserializes() {
    let schema_json = r#"{"type":"object","properties":{"tags":{"type":"array","items":{"type":"string"},"uniqueItems":true}},"required":["tags"]}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(schema_json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let generated = output.per_schema[0].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), &generated).expect("write lib.rs");
    let main_rs = r##"fn main() {
    use std::collections::HashSet;
    let root: compile_test::Root = serde_json::from_str(r#"{"tags":["a","b","c"]}"#).unwrap();
    let expected: HashSet<String> = ["a","b","c"].into_iter().map(String::from).collect();
    assert_eq!(root.tags, expected);
}
"##;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn generated_rust_array_optional_builds_and_deserializes() {
    let schema_json =
        r#"{"type":"object","properties":{"tags":{"type":"array","items":{"type":"string"}}}}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(schema_json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let generated = output.per_schema[0].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), &generated).expect("write lib.rs");
    let main_rs = r##"fn main() {
    let root: compile_test::Root = serde_json::from_str(r#"{"tags":["x","y"]}"#).unwrap();
    assert_eq!(root.tags, Some(vec!["x".to_string(), "y".to_string()]));
}
"##;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn generated_rust_min_items_max_items_builds_and_deserializes() {
    let schema_json = r#"{"type":"object","properties":{"tags":{"type":"array","items":{"type":"string"},"minItems":2,"maxItems":5}},"required":["tags"]}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(schema_json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let generated = output.per_schema[0].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), &generated).expect("write lib.rs");
    let main_rs = r##"fn main() {
    let root: compile_test::Root = serde_json::from_str(r#"{"tags":["a","b","c"]}"#).unwrap();
    assert_eq!(root.tags, vec!["a".to_string(), "b".to_string(), "c".to_string()]);
}
"##;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn generated_rust_optional_float_builds_and_deserializes() {
    let schema_json = r#"{"type":"object","properties":{"value":{"type":"number"}}}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(schema_json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let generated = output.per_schema[0].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), &generated).expect("write lib.rs");
    let main_rs = r##"fn main() {
    let root: compile_test::Root = serde_json::from_str(r#"{"value":2.5}"#).unwrap();
    assert_eq!(root.value, Some(2.5));
}
"##;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn round_trip_generated_rust_with_integer_json_schema_matches_original() {
    let schema_json = r#"{"type":"object","properties":{"id":{"type":"string"},"count":{"type":"integer"}},"required":["id"],"title":"Root"}"#;
    let settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let original: JsonSchema = parse_schema(schema_json, &settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output =
        generate_rust(std::slice::from_ref(&original), &code_gen_settings).expect("generate");
    let generated = output.per_schema[0].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), &generated).expect("write lib.rs");
    let main_rs = r#"
fn main() {
    use json_schema_rs::{JsonSchema, ToJsonSchema, parse_schema, JsonSchemaSettings};
    let schema = compile_test::Root::json_schema();
    let json: String = (&schema).try_into().expect("serialize");
    let settings = JsonSchemaSettings::builder().build();
    let reparsed: JsonSchema = parse_schema(&json, &settings).expect("parse");
    assert_eq!(schema, reparsed, "round-trip Root::json_schema() must match");
}
"#;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn round_trip_generated_rust_with_number_json_schema_matches_original() {
    let schema_json = r#"{"type":"object","properties":{"id":{"type":"string"},"count":{"type":"integer"},"value":{"type":"number"}},"required":["id"],"title":"Root"}"#;
    let settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let original: JsonSchema = parse_schema(schema_json, &settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output =
        generate_rust(std::slice::from_ref(&original), &code_gen_settings).expect("generate");
    let generated = output.per_schema[0].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), &generated).expect("write lib.rs");
    let main_rs = r#"
fn main() {
    use json_schema_rs::{JsonSchema, ToJsonSchema, parse_schema, JsonSchemaSettings};
    let schema = compile_test::Root::json_schema();
    let json: String = (&schema).try_into().expect("serialize");
    let settings = JsonSchemaSettings::builder().build();
    let reparsed: JsonSchema = parse_schema(&json, &settings).expect("parse");
    assert_eq!(schema, reparsed, "round-trip Root::json_schema() must match");
}
"#;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn round_trip_generated_rust_integer_with_min_max_preserves_bounds() {
    let schema_json = r#"{"type":"object","properties":{"byte":{"type":"integer","minimum":0,"maximum":255}},"required":["byte"],"title":"Root"}"#;
    let settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let original: JsonSchema = parse_schema(schema_json, &settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output =
        generate_rust(std::slice::from_ref(&original), &code_gen_settings).expect("generate");
    let generated = output.per_schema[0].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), &generated).expect("write lib.rs");
    let main_rs = r#"
fn main() {
    use json_schema_rs::{JsonSchema, ToJsonSchema, parse_schema, JsonSchemaSettings};
    let schema = compile_test::Root::json_schema();
    let json: String = (&schema).try_into().expect("serialize");
    let settings = JsonSchemaSettings::builder().build();
    let reparsed: JsonSchema = parse_schema(&json, &settings).expect("parse");
    assert_eq!(schema, reparsed, "round-trip Root::json_schema() must match");
}
"#;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn round_trip_generated_rust_number_with_min_max_preserves_bounds() {
    let schema_json = r#"{"type":"object","properties":{"value":{"type":"number","minimum":0.5,"maximum":100.5}},"required":["value"],"title":"Root"}"#;
    let settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let original: JsonSchema = parse_schema(schema_json, &settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output =
        generate_rust(std::slice::from_ref(&original), &code_gen_settings).expect("generate");
    let generated = output.per_schema[0].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), &generated).expect("write lib.rs");
    // f32::MIN/MAX do not round-trip exactly through JSON, so assert structure and approximate min/max instead of full equality.
    let main_rs = r#"
fn main() {
    use json_schema_rs::{JsonSchema, ToJsonSchema, parse_schema, JsonSchemaSettings};
    let schema = compile_test::Root::json_schema();
    let json: String = (&schema).try_into().expect("serialize");
    let settings = JsonSchemaSettings::builder().build();
    let reparsed: JsonSchema = parse_schema(&json, &settings).expect("parse");
    let value_schema = schema.properties.get("value").expect("value property");
    let value_reparsed = reparsed.properties.get("value").expect("value property");
    assert_eq!(value_schema.type_, value_reparsed.type_, "type must match");
    let (min_s, max_s) = (value_schema.minimum.unwrap(), value_schema.maximum.unwrap());
    let (min_r, max_r) = (value_reparsed.minimum.unwrap(), value_reparsed.maximum.unwrap());
    let tol = 1e32_f64;
    assert!((min_s - min_r).abs() < tol && (max_s - max_r).abs() < tol, "min/max must round-trip within tolerance");
}
"#;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn integration_parse_and_generate_root_not_object_errors() {
    let json = r#"{"type":"string"}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let actual = generate_rust(&[schema], &code_gen_settings);
    assert!(actual.is_err(), "root not object should error");
}

/// Cargo.toml content for a temp crate that compiles generated Rust (needs json-schema-rs and macro for `ToJsonSchema` derive).
fn temp_crate_cargo_toml_with_reverse_codegen() -> String {
    let manifest_dir =
        std::env::var("CARGO_MANIFEST_DIR").expect("CARGO_MANIFEST_DIR set by Cargo");
    let workspace_root = std::path::Path::new(&manifest_dir)
        .parent()
        .expect("workspace root");
    let json_schema_rs_path = workspace_root.join("json_schema_rs");
    let macro_path = workspace_root.join("json_schema_rs_macro");
    format!(
        r#"[package]
name = "compile_test"
version = "0.0.1"
edition = "2024"

[lib]
path = "src/lib.rs"

[[bin]]
name = "run"
path = "src/main.rs"

[dependencies]
serde = {{ version = "1", features = ["derive"] }}
serde_json = "1"
json-schema-rs = {{ path = "{}" }}
json-schema-rs-macro = {{ path = "{}" }}
"#,
        json_schema_rs_path.display(),
        macro_path.display()
    )
}

/// Builds map: directory path (relative) -> set of module names. Root is `PathBuf::new()`; subdirs e.g. "nested".
fn mod_rs_content_by_dir(output_relatives: &[PathBuf]) -> BTreeMap<PathBuf, BTreeSet<String>> {
    let mut by_dir: BTreeMap<PathBuf, BTreeSet<String>> = BTreeMap::new();
    for rel in output_relatives {
        let path = Path::new(rel);
        let components: Vec<_> = path.components().collect();
        if components.is_empty() {
            continue;
        }
        let module_name = path
            .file_stem()
            .and_then(|s| s.to_str())
            .unwrap_or("schema")
            .to_string();
        if components.len() == 1 {
            by_dir
                .entry(PathBuf::new())
                .or_default()
                .insert(module_name);
        } else {
            let subdir_name = components[0].as_os_str().to_string_lossy().to_string();
            by_dir
                .entry(PathBuf::new())
                .or_default()
                .insert(subdir_name.clone());
            by_dir
                .entry(PathBuf::from(&subdir_name))
                .or_default()
                .insert(module_name);
        }
    }
    by_dir
}

#[test]
fn generated_rust_single_schema_builds_and_deserializes() {
    let schema_json =
        r#"{"type":"object","properties":{"id":{"type":"string"}},"required":["id"]}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(schema_json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let generated = output.per_schema[0].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), &generated).expect("write lib.rs");
    let main_rs = r##"fn main() {
    let _: compile_test::Root = serde_json::from_str(r#"{"id":"x"}"#).unwrap();
}
"##;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn generated_rust_description_builds_and_deserializes() {
    let schema_json = r#"{"type":"object","description":"Root","properties":{"id":{"type":"string","description":"Identifier"}},"required":["id"]}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(schema_json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let generated = output.per_schema[0].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), &generated).expect("write lib.rs");
    let main_rs = r##"fn main() {
    let _: compile_test::Root = serde_json::from_str(r#"{"id":"x"}"#).unwrap();
}
"##;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn generated_rust_required_integer_builds_and_deserializes() {
    let schema_json =
        r#"{"type":"object","properties":{"count":{"type":"integer"}},"required":["count"]}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(schema_json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let generated = output.per_schema[0].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), &generated).expect("write lib.rs");
    let main_rs = r##"fn main() {
    let root: compile_test::Root = serde_json::from_str(r#"{"count":42}"#).unwrap();
    assert_eq!(root.count, 42);
}
"##;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn generated_rust_optional_integer_builds_and_deserializes() {
    let schema_json = r#"{"type":"object","properties":{"count":{"type":"integer"}}}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(schema_json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let generated = output.per_schema[0].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), &generated).expect("write lib.rs");
    let main_rs = r##"fn main() {
    let root: compile_test::Root = serde_json::from_str(r#"{"count":100}"#).unwrap();
    assert_eq!(root.count, Some(100));
}
"##;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn generated_rust_integer_with_min_max_u8_builds_and_deserializes() {
    let schema_json = r#"{"type":"object","properties":{"byte":{"type":"integer","minimum":0,"maximum":255}},"required":["byte"]}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(schema_json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let generated = output.per_schema[0].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), &generated).expect("write lib.rs");
    let main_rs = r##"fn main() {
    let root: compile_test::Root = serde_json::from_str(r#"{"byte":42}"#).unwrap();
    assert_eq!(root.byte, 42u8);
}
"##;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn generated_rust_number_with_min_max_f32_builds_and_deserializes() {
    let schema_json = r#"{"type":"object","properties":{"value":{"type":"number","minimum":0.5,"maximum":100.5}},"required":["value"]}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(schema_json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let generated = output.per_schema[0].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), &generated).expect("write lib.rs");
    let main_rs = r##"fn main() {
    let root: compile_test::Root = serde_json::from_str(r#"{"value":50.5}"#).unwrap();
    assert!((root.value - 50.5_f32).abs() < 0.001);
}
"##;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn generated_rust_nested_modules_builds_and_deserializes() {
    let schema_root = r#"{"type":"object","properties":{"x":{"type":"string"}}}"#;
    let schema_child = r#"{"type":"object","properties":{"y":{"type":"string"}}}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let parsed_root: JsonSchema = parse_schema(schema_root, &schema_settings).expect("parse root");
    let parsed_child: JsonSchema =
        parse_schema(schema_child, &schema_settings).expect("parse child");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[parsed_root, parsed_child], &code_gen_settings).expect("generate");

    let output_relatives: Vec<PathBuf> =
        vec![PathBuf::from("root.rs"), PathBuf::from("nested/child.rs")];
    let by_dir = mod_rs_content_by_dir(&output_relatives);

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    let nested_src = src.join("nested");
    fs::create_dir_all(&nested_src).expect("create src and nested");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");

    let root_modules = by_dir.get(&PathBuf::new()).expect("root entry");
    let lib_rs_content: String = root_modules
        .iter()
        .map(|m| format!("pub mod {m};"))
        .collect::<Vec<_>>()
        .join("\n")
        + "\n";
    fs::write(src.join("lib.rs"), lib_rs_content).expect("write lib.rs");
    fs::write(src.join("root.rs"), &output.per_schema[0]).expect("write root.rs");

    let nested_modules = by_dir.get(&PathBuf::from("nested")).expect("nested entry");
    let nested_mod_content: String = nested_modules
        .iter()
        .map(|m| format!("pub mod {m};"))
        .collect::<Vec<_>>()
        .join("\n")
        + "\n";
    fs::write(nested_src.join("mod.rs"), nested_mod_content).expect("write nested/mod.rs");
    fs::write(nested_src.join("child.rs"), &output.per_schema[1]).expect("write nested/child.rs");

    let main_rs = r##"fn main() {
    let _: compile_test::root::Root = serde_json::from_str(r#"{"x":"ok"}"#).unwrap();
    let _: compile_test::nested::child::Root = serde_json::from_str(r#"{"y":"ok"}"#).unwrap();
}
"##;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn generated_rust_multi_schema_builds_and_deserializes() {
    let schema_a = r#"{"type":"object","properties":{"a":{"type":"string"}}}"#;
    let schema_b = r#"{"type":"object","properties":{"b":{"type":"string"}}}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let parsed_a: JsonSchema = parse_schema(schema_a, &schema_settings).expect("parse a");
    let parsed_b: JsonSchema = parse_schema(schema_b, &schema_settings).expect("parse b");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[parsed_a, parsed_b], &code_gen_settings).expect("generate");

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), "pub mod a;\npub mod b;\n").expect("write lib.rs");
    fs::write(src.join("a.rs"), &output.per_schema[0]).expect("write a.rs");
    fs::write(src.join("b.rs"), &output.per_schema[1]).expect("write b.rs");
    let main_rs = r##"fn main() {
    let _: compile_test::a::Root = serde_json::from_str(r#"{"a":"x"}"#).unwrap();
    let _: compile_test::b::Root = serde_json::from_str(r#"{"b":"y"}"#).unwrap();
}
"##;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn generated_rust_optional_string_builds_and_deserializes() {
    let schema_json = r#"{"type":"object","properties":{"name":{"type":"string"}}}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(schema_json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let generated = output.per_schema[0].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), &generated).expect("write lib.rs");
    let main_rs = r##"fn main() {
    let v: compile_test::Root = serde_json::from_str(r#"{}"#).unwrap();
    assert!(v.name.is_none());
    let v2: compile_test::Root = serde_json::from_str(r#"{"name":"x"}"#).unwrap();
    assert_eq!(v2.name.as_deref(), Some("x"));
}
"##;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn generated_rust_nested_object_builds_and_deserializes() {
    let schema_json = r#"{"type":"object","properties":{"address":{"type":"object","properties":{"city":{"type":"string"},"street_address":{"type":"string"}}}}}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(schema_json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let generated = output.per_schema[0].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), &generated).expect("write lib.rs");
    let main_rs = r##"fn main() {
    let v: compile_test::Root = serde_json::from_str(r#"{"address":{"city":"NYC","street_address":"123 Main"}}"#).unwrap();
    assert!(v.address.is_some());
    let addr = v.address.as_ref().unwrap();
    assert_eq!(addr.city.as_deref(), Some("NYC"));
    assert_eq!(addr.street_address.as_deref(), Some("123 Main"));
}
"##;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn generated_rust_enum_builds_and_deserializes() {
    let schema_json = r#"{"type":"object","properties":{"status":{"enum":["open","closed"]}},"required":["status"]}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(schema_json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let generated = output.per_schema[0].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), &generated).expect("write lib.rs");
    let main_rs = r##"fn main() {
    let root: compile_test::Root = serde_json::from_str(r#"{"status":"open"}"#).unwrap();
    assert_eq!(root.status, compile_test::Status::Open);
    let root2: compile_test::Root = serde_json::from_str(r#"{"status":"closed"}"#).unwrap();
    assert_eq!(root2.status, compile_test::Status::Closed);
}
"##;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn generated_rust_enum_collision_builds_and_deserializes() {
    let schema_json = r#"{"type":"object","properties":{"t":{"enum":["a","A"]}},"required":["t"]}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(schema_json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let generated = output.per_schema[0].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), &generated).expect("write lib.rs");
    let main_rs = r##"fn main() {
    let root: compile_test::Root = serde_json::from_str(r#"{"t":"a"}"#).unwrap();
    assert_eq!(root.t, compile_test::T::A_1);
    let root2: compile_test::Root = serde_json::from_str(r#"{"t":"A"}"#).unwrap();
    assert_eq!(root2.t, compile_test::T::A_0);
}
"##;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn generated_rust_enum_dedupe_builds_and_deserializes() {
    let schema_json = r#"{"type":"object","properties":{"a":{"enum":["x","y"]},"b":{"enum":["x","y"]}},"required":["a"]}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(schema_json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let generated = output.per_schema[0].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), &generated).expect("write lib.rs");
    let main_rs = r##"fn main() {
    let root: compile_test::Root = serde_json::from_str(r#"{"a":"x","b":"y"}"#).unwrap();
    assert_eq!(root.a, compile_test::A::X);
    assert_eq!(root.b, Some(compile_test::A::Y));
}
"##;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn generated_rust_enum_duplicate_values_builds_and_deserializes() {
    let schema_json = r#"{"type":"object","properties":{"t":{"enum":["A","A","A","a","a","a","a","a","a","a"]}},"required":["t"]}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(schema_json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let generated = output.per_schema[0].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), &generated).expect("write lib.rs");
    let main_rs = r##"fn main() {
    let root: compile_test::Root = serde_json::from_str(r#"{"t":"A"}"#).unwrap();
    assert_eq!(root.t, compile_test::T::A_0);
    let root2: compile_test::Root = serde_json::from_str(r#"{"t":"a"}"#).unwrap();
    assert_eq!(root2.t, compile_test::T::A_1);
}
"##;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn generated_rust_hyphenated_property_builds_and_deserializes() {
    let schema_json = r#"{"type":"object","properties":{"foo-bar":{"type":"string"}}}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(schema_json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let generated = output.per_schema[0].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), &generated).expect("write lib.rs");
    let main_rs = r##"fn main() {
    let v: compile_test::Root = serde_json::from_str(r#"{"foo-bar":"baz"}"#).unwrap();
    assert_eq!(v.foo_bar.as_deref(), Some("baz"));
}
"##;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn generated_rust_hyphenated_paths_builds_and_deserializes() {
    let schema_a = r#"{"type":"object","properties":{"a":{"type":"string"}}}"#;
    let schema_b = r#"{"type":"object","properties":{"b":{"type":"string"}}}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let parsed_a: JsonSchema = parse_schema(schema_a, &schema_settings).expect("parse a");
    let parsed_b: JsonSchema = parse_schema(schema_b, &schema_settings).expect("parse b");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[parsed_a, parsed_b], &code_gen_settings).expect("generate");

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    let sub_dir_src = src.join("sub_dir");
    fs::create_dir_all(&sub_dir_src).expect("create src and sub_dir");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), "pub mod schema_1;\npub mod sub_dir;\n").expect("write lib.rs");
    fs::write(src.join("schema_1.rs"), &output.per_schema[0]).expect("write schema_1.rs");
    fs::write(sub_dir_src.join("mod.rs"), "pub mod schema_2;\n").expect("write sub_dir/mod.rs");
    fs::write(sub_dir_src.join("schema_2.rs"), &output.per_schema[1])
        .expect("write sub_dir/schema_2.rs");
    let main_rs = r##"fn main() {
    let _: compile_test::schema_1::Root = serde_json::from_str(r#"{"a":"x"}"#).unwrap();
    let _: compile_test::sub_dir::schema_2::Root = serde_json::from_str(r#"{"b":"y"}"#).unwrap();
}
"##;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn generated_rust_dedupe_two_identical_builds_and_deserializes() {
    let schema_json = r#"{"type":"object","properties":{"id":{"type":"string"}}}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let parsed_a: JsonSchema = parse_schema(schema_json, &schema_settings).expect("parse a");
    let parsed_b: JsonSchema = parse_schema(schema_json, &schema_settings).expect("parse b");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder()
        .dedupe_mode(DedupeMode::Full)
        .build();
    let output = generate_rust(&[parsed_a, parsed_b], &code_gen_settings).expect("generate");
    let shared_bytes: Vec<u8> = output
        .shared
        .clone()
        .expect("dedupe with two identical schemas produces shared");
    let per_a = output.per_schema[0].clone();
    let per_b = output.per_schema[1].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(
        src.join("lib.rs"),
        "pub mod shared;\npub mod a;\npub mod b;\n",
    )
    .expect("write lib.rs");
    fs::write(src.join("shared.rs"), &shared_bytes).expect("write shared.rs");
    let a_rs_content: String = String::from_utf8(per_a)
        .expect("utf8")
        .replace("pub use crate::", "pub use crate::shared::");
    let b_rs_content: String = String::from_utf8(per_b)
        .expect("utf8")
        .replace("pub use crate::", "pub use crate::shared::");
    fs::write(src.join("a.rs"), a_rs_content.as_bytes()).expect("write a.rs");
    fs::write(src.join("b.rs"), b_rs_content.as_bytes()).expect("write b.rs");
    let main_rs = r##"fn main() {
    let _: compile_test::a::Root = serde_json::from_str(r#"{"id":"x"}"#).unwrap();
    let _: compile_test::b::Root = serde_json::from_str(r#"{"id":"y"}"#).unwrap();
}
"##;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn generated_rust_model_name_source_property_key_builds_and_deserializes() {
    let schema_json = r#"{"type":"object","properties":{"address":{"type":"object","title":"FooBar","properties":{"city":{"type":"string"}}}}}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(schema_json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder()
        .model_name_source(ModelNameSource::PropertyKeyFirst)
        .build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let generated = output.per_schema[0].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), &generated).expect("write lib.rs");
    let main_rs = r##"fn main() {
    let v: compile_test::Root = serde_json::from_str(r#"{"address":{"city":"NYC"}}"#).unwrap();
    assert!(v.address.is_some());
    assert_eq!(v.address.as_ref().unwrap().city.as_deref(), Some("NYC"));
}
"##;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn generated_rust_deep_nesting_builds_and_deserializes() {
    const DEPTH: usize = 15;
    let mut inner: JsonSchema = JsonSchema {
        type_: Some("object".to_string()),
        properties: {
            let mut m = std::collections::BTreeMap::new();
            m.insert(
                "value".to_string(),
                JsonSchema {
                    type_: Some("string".to_string()),
                    properties: std::collections::BTreeMap::new(),
                    required: None,
                    title: None,
                    description: None,
                    enum_values: None,
                    items: None,
                    unique_items: None,
                    min_items: None,
                    max_items: None,
                    minimum: None,
                    maximum: None,
                    min_length: None,
                    max_length: None,
                },
            );
            m
        },
        required: None,
        title: Some("Leaf".to_string()),
        description: None,
        enum_values: None,
        items: None,
        unique_items: None,
        min_items: None,
        max_items: None,
        minimum: None,
        maximum: None,
        min_length: None,
        max_length: None,
    };
    for i in (0..DEPTH).rev() {
        let mut wrap: JsonSchema = JsonSchema {
            type_: Some("object".to_string()),
            properties: std::collections::BTreeMap::new(),
            required: None,
            title: Some(format!("Level{i}")),
            description: None,
            enum_values: None,
            items: None,
            unique_items: None,
            min_items: None,
            max_items: None,
            minimum: None,
            maximum: None,
            min_length: None,
            max_length: None,
        };
        wrap.properties.insert("child".to_string(), inner);
        inner = wrap;
    }
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[inner], &code_gen_settings).expect("generate");
    let generated = output.per_schema[0].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), &generated).expect("write lib.rs");
    let main_rs = r##"fn main() {
    let _: compile_test::Level0 = serde_json::from_str(r#"{"child":{"child":{"child":{}}}}"#).unwrap();
}
"##;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn generated_rust_string_min_length_max_length_builds_and_deserializes() {
    let schema_json = r#"{"type":"object","properties":{"name":{"type":"string","minLength":2,"maxLength":50}},"required":["name"]}"#;
    let schema_settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let schema: JsonSchema = parse_schema(schema_json, &schema_settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output = generate_rust(&[schema], &code_gen_settings).expect("generate");
    let generated = output.per_schema[0].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), &generated).expect("write lib.rs");
    let main_rs = r##"fn main() {
    let root: compile_test::Root = serde_json::from_str(r#"{"name":"Alice"}"#).unwrap();
    assert_eq!(root.name, "Alice".to_string());
}
"##;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}

#[test]
fn round_trip_generated_rust_string_min_length_max_length_json_schema_matches_original() {
    let schema_json = r#"{"type":"object","properties":{"name":{"type":"string","minLength":2,"maxLength":50}},"required":["name"]}"#;
    let settings: JsonSchemaSettings = JsonSchemaSettings::builder().build();
    let original: JsonSchema = parse_schema(schema_json, &settings).expect("parse schema");
    let code_gen_settings: CodeGenSettings = CodeGenSettings::builder().build();
    let output =
        generate_rust(std::slice::from_ref(&original), &code_gen_settings).expect("generate");
    let generated = output.per_schema[0].clone();

    let temp_dir = tempfile::tempdir().expect("temp dir");
    let src = temp_dir.path().join("src");
    fs::create_dir_all(&src).expect("create src");

    let cargo_toml = temp_crate_cargo_toml_with_reverse_codegen();
    fs::write(temp_dir.path().join("Cargo.toml"), cargo_toml).expect("write Cargo.toml");
    fs::write(src.join("lib.rs"), &generated).expect("write lib.rs");
    let main_rs = r#"
fn main() {
    use json_schema_rs::{JsonSchema, ToJsonSchema, parse_schema, JsonSchemaSettings};
    let schema = compile_test::Root::json_schema();
    let json: String = (&schema).try_into().expect("serialize");
    let settings = JsonSchemaSettings::builder().build();
    let reparsed: JsonSchema = parse_schema(&json, &settings).expect("parse");
    assert_eq!(schema, reparsed, "round-trip Root::json_schema() must match");
}
"#;
    fs::write(src.join("main.rs"), main_rs).expect("write main.rs");

    let build = Command::new("cargo")
        .args(["build"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo build");
    assert!(
        build.status.success(),
        "cargo build failed: stderr={}",
        String::from_utf8_lossy(&build.stderr)
    );

    let run = Command::new("cargo")
        .args(["run", "--bin", "run"])
        .current_dir(temp_dir.path())
        .output()
        .expect("run cargo run");
    assert!(
        run.status.success(),
        "cargo run failed: stderr={}",
        String::from_utf8_lossy(&run.stderr)
    );
}
